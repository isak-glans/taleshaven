@page "/campaigns/{CampaignId:int}/ooc"
@attribute [Authorize]
@rendermode InteractiveServer

<CampaignChannel CampaignId="CampaignId" Kind="ThreadKind.Ooc" />

@code {
    [Parameter]
    public int CampaignId { get; set; }
}
