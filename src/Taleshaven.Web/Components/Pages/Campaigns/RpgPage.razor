@page "/campaigns/{CampaignId:int}"
@attribute [Authorize]
@rendermode InteractiveServer

@* Kampanjens startsida: RPG-chatten. *@

<CampaignChannel CampaignId="CampaignId" Kind="ThreadKind.Rpg" />

@code {
    [Parameter]
    public int CampaignId { get; set; }
}
